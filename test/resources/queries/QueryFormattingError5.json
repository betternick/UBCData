{
  "title": "Query formatting issue - subject/Subject instead of dept which is the Dataset version of what's called Subject in file",
  "input": {
    "WHERE": {
      "AND":
      [
        {"LT": {"sections_avg": 99}},
        {"IS": {"sections_Subject": "aanb"}}
      ]
    },
    "OPTIONS": {
      "COLUMNS": [
        "sections_subject",
        "sections_avg"
      ],
      "ORDER": "sections_avg"
    }
  },
  "errorExpected": true,
  "expected": "InsightError"
}

